/** @type {import('next').NextConfig} */
const nextConfig = {
  // ✅ All your previous config keys (if any) go here.
  // Remove *any* `experimental.ppr` line.
  // Example:
  // images: { domains: ['example.com'] },
};

module.exports = nextConfig;
